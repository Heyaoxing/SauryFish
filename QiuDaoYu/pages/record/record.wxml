
<scroll-view scroll-top="{{scrollTop}}" scroll-y="true" style="height:{{scrollHeight}}px;" 
       bindscrolltolower="bindDownLoad" bindscroll="scroll" bindscrolltoupper="refresh">
<view class="search">
    <input placeholder="搜索姓名/打卡机号" auto-focus confirm-type="search" bindconfirm="confirmSubmit" />
  </view>

    <view wx:for="{{items}}">
    <text>{{item.name}}</text> <text>{{item.state}}</text> <text>{{item.attendancedOn}}</text>
  </view>
</scroll-view>
